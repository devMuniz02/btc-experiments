name: Fixed 5K Sync Azure Train Requests (Disabled)

on:
  workflow_dispatch:
    inputs:
      retry_failed:
        description: "Retry request files currently marked failed"
        required: false
        default: false
        type: boolean

permissions:
  contents: write

jobs:
  reconcile:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "Azure reconcile workflow is intentionally disabled while fixed-5k training runs locally and only publishes artifacts to Azure."
