name: Fixed 5K Submit Azure Train Request (Disabled)

on:
  workflow_dispatch:
    inputs:
      request_file:
        description: "Repo-relative request JSON path under train_requests/requests/"
        required: true
        type: string

permissions:
  contents: write

jobs:
  submit-request:
    if: false
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: echo "Azure submission workflow is intentionally disabled while fixed-5k training runs locally and only publishes artifacts to Azure."
